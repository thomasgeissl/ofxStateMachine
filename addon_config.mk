meta:
	ADDON_NAME = ofxStateMachine
	ADDON_DESCRIPTION = Addon that manages transitions between states
	ADDON_AUTHOR = Thomas Geissl
	ADDON_TAGS = "state machine" "fsm" "states" "transitions"
	ADDON_URL = http://github.com/thomasgeissl/ofxStateMachine
